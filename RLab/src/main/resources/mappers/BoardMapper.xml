<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.RLab.dao.BoardDAO">
	<!-- 게시글 등록 -->
	<insert id="insertBoard">
    insert into board(bo_st_num, bo_title, bo_content, bo_me_id, bo_reg_date)
    	values(#{board.bo_st_num}, #{board.bo_title}, #{board.bo_content}, #{board.bo_me_id}, NOW())
	</insert>
	
	<select id="selectBoardList" resultType="kr.kh.RLab.vo.BoardVO">
		select 
			*
		from 
			board
		join study
		on  bo_st_num = st_num
		join member 
		on  bo_me_id = me_id
		order by bo_reg_date desc
	</select>
	
	
	
	

	
</mapper>